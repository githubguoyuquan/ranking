{{- if .Values.externalSecrets.enabled }}
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ include "ranking.fullname" . }}-env
  labels:
    {{- include "ranking.labels" . | nindent 4 }}
spec:
  refreshInterval: {{ .Values.externalSecrets.refreshInterval | default "1h" }}
  secretStoreRef:
    name: {{ .Values.externalSecrets.secretStoreRef.name }}
    kind: {{ .Values.externalSecrets.secretStoreRef.kind }}
  target:
    name: {{ .Values.secret.name }}
    creationPolicy: Owner
  data:
    {{- range .Values.externalSecrets.data }}
    - secretKey: {{ .secretKey }}
      remoteRef:
        key: {{ .remoteRef.key }}
        {{- if .remoteRef.property }}
        property: {{ .remoteRef.property }}
        {{- end }}
    {{- end }}
    {{- if .Values.drReadinessCron.enabled }}
    - secretKey: RANKING_ADMIN_API_KEY
      remoteRef:
        key: {{ .Values.externalSecrets.adminApiKeySecret | default "prod/ranking/admin" }}
        property: api_key
    {{- end }}
{{- end }}
