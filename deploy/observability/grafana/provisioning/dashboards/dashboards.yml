apiVersion: 1
providers:
  - name: ranking
    orgId: 1
    folder: Ranking
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
