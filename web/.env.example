# Nest API（与浏览器请求、Server Components 一致；未设置时默认同 src/lib/api.ts 的 DEFAULT_PUBLIC_API_URL）
NEXT_PUBLIC_API_URL=http://localhost:3000

# C 端只读 API Key（生产 API_AUTH_REQUIRED=true 时使用 read 作用域密钥）
# NEXT_PUBLIC_READ_API_KEY=rk_...

# 管理台 / 通用 API Key（可选；合规页等仍可使用）
# NEXT_PUBLIC_API_KEY=rk_...
